import React from 'react';
import ReactDOM from 'react-dom';
import List from '../list';

// let updateSearchByName = (event) => {
//     let searchByName = event.target.value;

//     return searchByName;
// }

// let SearchBar = ({ items }) => {
//     let searchedItems = this.items.filter(
//         (item) => {
//             return item.name.toLowerCase().indexOf(searchByName.toLowerCase()) !== -1;
//         }   
//     );

//     return (
//         <input 
//             type="text" 
//             value={this.state.searchByName} 
//             onChange={this.updateSearchByName.bind(this)} 
//             className="filters-container__searchbar"
//             placeholder="Search..."
//         />
//         <List items={searchedItems} />
//     );
// }

// export default SearchBar;